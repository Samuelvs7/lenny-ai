# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0

# --- HTTP / ingestion ---
httpx==0.28.1
PyYAML==6.0.2

# --- Retrieval ---
numpy==2.2.1

# --- Artifact safety ---
# nh3 is a Rust (ammonia) HTML sanitiser: no C toolchain needed, ships wheels
# for every platform we target, and is materially faster than bleach.
nh3==0.2.20

# --- Observability ---
structlog==24.4.0

# --- Cloud model provider (optional at runtime; import is guarded) ---
anthropic==0.42.0
